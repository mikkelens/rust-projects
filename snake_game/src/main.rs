extern crate glutin_window;
extern crate graphics;
extern crate opengl_graphics;
extern crate piston;

// use opengl_graphics::OpenGL;

fn main() {
	// let opengl = OpenGL::V3_2;

	// let mut window: GlutinWindow = WindowSettings::new("Snake Game", [300,
	// 300]) 	.opengl(opengl)
	// 	.exit_on_esc(true)
	// 	.build()
	// 	.unwrap();
}
